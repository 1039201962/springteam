<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.st.springstore.pay.dao.PayDao">
	<update id="doPay">
		update user set money=money-#{money} where id =#{userId}
	</update>
	<update id="doPayOrder">
	update  order_list set pay_status = 1 where order_num =#{orderId}
	</update>
</mapper>
